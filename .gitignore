node_modules
dist
*.log
.DS_Store

# Distributable artifact: regenerated from data/Dataset.bin by the build (lives at
# the repo root to avoid a case-only clash with data/Dataset.bin). Shipped to npm
# via the "files" field, but not committed to git.
/dataset.bin
